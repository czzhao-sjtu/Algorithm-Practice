#include <tuple>
#include <vector>
using namespace std;

#ifndef _PACKPROBLEM
#define _PACKPROBLEM

tuple<vector<int>, int> maxValues(vector<int> values, vector<int> weights, int maxCapacity);

#endif